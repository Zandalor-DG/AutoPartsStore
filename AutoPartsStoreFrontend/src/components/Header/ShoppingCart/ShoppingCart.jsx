import React from "react";
import c from "./ShoppingCart.module.css";

const ShoppingCart = (props) => {
  return (
    <div className="header">
      <input className={c.img} type="button" />
    </div>
  );
};

export default ShoppingCart;
